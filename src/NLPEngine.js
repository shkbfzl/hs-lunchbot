/**
 * Created by mohamed.kante on 9/19/16.
 */

require('rootpath')();

var log = require("log4js").getLogger(__filename);
var config = require("config");

var parsedFile ;
var parsedFile ;

module.exports = {

    init: function(routeFile) {

        log.debug("Loading NLP route file: ")
    }
}
